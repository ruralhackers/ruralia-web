---
import Base from '../layouts/Base.astro';

const schema = {
  '@context': 'https://schema.org',
  '@type': 'Organization',
  name: 'Rural IA',
  url: 'https://ruralia.gal',
  description: 'Retiro inmersivo de IA aplicada en el rural gallego.',
  foundingDate: '2026',
  sameAs: [],
};

const days = [
  {
    num: '01',
    title: 'Fundamentos IA aplicada',
    desc: 'Prompting avanzado, elegir la herramienta correcta, casos de uso reales.',
  },
  {
    num: '02',
    title: 'Asistentes y herramientas de IA',
    desc: 'GPTs, Claude Projects, Claude Code, Cursor. Configura IAs que trabajan para ti.',
  },
  {
    num: '03',
    title: 'Construir con IA',
    desc: 'Cursor, Bolt, v0: de idea a prototipo funcional en una tarde.',
  },
  {
    num: '04',
    title: 'Tu stack personalizado',
    desc: 'Monta tu propio setup de IA. Sesión guiada + demo final al grupo.',
  },
];

const stats = [
  { num: '600K', label: 'profesionales tech en España sin formación en IA' },
  { num: '+56%', label: 'prima salarial para quienes dominan IA' },
  { num: '0', label: 'retiros inmersivos de IA para techies en España' },
];
---

<Base
  title="Rural IA — Retiro inmersivo de IA aplicada en Galicia"
  description="4 días en el rural gallego para transformar tu forma de trabajar con IA. Formación práctica, alojamiento y comidas incluidas. 12 plazas por edición."
  schema={schema}
>
  <!-- ==================== S1: Hero ==================== -->
  <section class="hero bg-crema">
    <div class="hero-inner">
      <p class="hero-badge">Junio 2026 · Anceu Coliving, Pontevedra</p>
      <img src="/images/rural-ia-logo-crop-removebg.png" alt="Rural IA" class="hero-logo" />
      <h1>4 días en Galicia para transformar tu forma de trabajar con IA</h1>
      <p class="hero-sub">
        Retiro inmersivo para freelancers y profesionales tech.
        Formación práctica de IA aplicada, alojamiento y comidas incluidas. 12 plazas.
      </p>
      <div class="hero-cta">
        <a href="/retiro-ia-galicia/#reservar" class="btn btn-primary btn-lg">Reserva tu plaza</a>
      </div>
      <p class="hero-price">Desde 697 EUR todo incluido</p>
    </div>
  </section>

  <!-- ==================== S2: Foto ancho completo ==================== -->
  <section class="fullwidth-photo">
    <img
      src="/images/ruralia-1.jpg"
      alt="Anceu Coliving entre árboles con niebla matutina en Galicia"
      loading="lazy"
    />
  </section>

  <!-- ==================== S3: Problema ==================== -->
  <section class="section bg-niebla">
    <div class="section-inner text-center">
      <h2>Sabes que la IA va a cambiarlo todo</h2>
      <p>
        Pero no encuentras el momento. Los cursos online se abandonan.
        Los bootcamps duran meses y cuestan miles de euros.
        Y cada semana salen 3 herramientas nuevas que no sabes si probar o ignorar.
      </p>
      <p class="highlight-quote">
        Lo que necesitas no es más teoría.<br />
        Es sentarte 4 días a construir cosas reales con IA.
      </p>
    </div>
  </section>

  <!-- ==================== S4: Solución ==================== -->
  <section class="section bg-crema">
    <div class="section-inner text-center">
      <h2>Un retiro inmersivo de IA, en un pueblo gallego</h2>
      <p>
        4 días de formación práctica en Anceu Coliving.
        Vienes con tu portátil, te vas con herramientas de IA configuradas y funcionando en tu trabajo.
        Sin humo, sin teoría innecesaria.
        Rodeado de naturaleza, buena comida y gente como tú.
      </p>
    </div>
  </section>

  <!-- ==================== S5: Programa ==================== -->
  <section class="section bg-niebla">
    <div class="section-inner text-center">
      <h2>4 días, 4 bloques</h2>
    </div>
    <div class="program-grid">
      {days.map((day) => (
        <div class="program-day">
          <span class="day-label">Día {day.num}</span>
          <h3>{day.title}</h3>
          <p>{day.desc}</p>
        </div>
      ))}
    </div>
    <div class="section-inner text-center">
      <p class="program-link">
        <a href="/retiro-ia-galicia/">Ver programa completo →</a>
      </p>
    </div>
  </section>

  <!-- ==================== S6: Stats ==================== -->
  <section class="section bg-crema">
    <div class="stats-row">
      {stats.map((stat) => (
        <div class="stat">
          <span class="stat-num">{stat.num}</span>
          <span class="stat-label">{stat.label}</span>
        </div>
      ))}
    </div>
  </section>

  <!-- ==================== S7: Para quién (preview) ==================== -->
  <section class="section bg-niebla">
    <div class="section-inner text-center">
      <h2>Para freelancers, indie hackers y profesionales tech</h2>
      <p>
        Si te buscas la vida con tecnología y quieres integrar IA en tu trabajo, esto es para ti.
        No hace falta saber programar. Sí hace falta tener ganas de cacharrear.
      </p>
      <p class="discover-link">
        <a href="/para-quien/">Descubre si es para ti →</a>
      </p>
    </div>
  </section>

  <!-- ==================== S8: CTA Final ==================== -->
  <section class="cta-final bg-dark">
    <div class="cta-final-inner">
      <h2>Solo 12 plazas por edición</h2>
      <p>Primera edición: junio 2026 en Anceu Coliving, Pontevedra.</p>
      <a href="/retiro-ia-galicia/#reservar" class="btn btn-gold btn-lg">Reserva tu plaza</a>
    </div>
  </section>
</Base>

<style>
  /* ===== Hero ===== */
  .hero {
    padding: 5rem 1.5rem 6rem;
    text-align: center;
  }
  .hero-inner {
    max-width: 780px;
    margin: 0 auto;
  }
  .hero-badge {
    display: inline-block;
    font-size: 0.8rem;
    color: var(--verde-hoja);
    text-transform: uppercase;
    letter-spacing: 0.12em;
    font-weight: 600;
    margin: 0 0 2rem;
    padding: 0.4rem 1rem;
    border: 1px solid var(--verde-hoja);
    border-radius: 100px;
  }
  .hero-logo {
    height: 48px;
    width: auto;
    margin: 0 auto 1.5rem;
  }
  .hero h1 {
    margin: 0 0 1.5rem;
    color: var(--verde-bosque);
  }
  .hero-sub {
    font-size: 1.15rem;
    color: var(--color-text-secondary);
    line-height: 1.7;
    margin: 0 0 2.5rem;
    max-width: 620px;
    margin-left: auto;
    margin-right: auto;
  }
  .hero-cta {
    margin-bottom: 1rem;
  }
  .hero-price {
    color: var(--marron-claro);
    font-size: 0.95rem;
    margin: 0;
  }

  /* ===== Full-width Photo ===== */
  .fullwidth-photo {
    width: 100%;
    overflow: hidden;
    line-height: 0;
  }
  .fullwidth-photo img {
    width: 100%;
    height: auto;
    max-height: 520px;
    object-fit: cover;
  }

  /* ===== Problem section — Highlight quote ===== */
  .highlight-quote {
    font-family: var(--font-heading);
    font-size: clamp(1.3rem, 3vw, 1.65rem);
    color: var(--verde-bosque);
    font-weight: 700;
    font-style: normal;
    letter-spacing: -0.03em;
    line-height: 1.4;
    margin-top: 2rem;
    margin-bottom: 0;
  }

  /* ===== Program ===== */
  .program-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.25rem;
    max-width: 720px;
    margin: 2rem auto 2rem;
    padding: 0 1.5rem;
  }
  .program-day {
    border: 1px solid var(--verde-hoja);
    border-radius: 8px;
    padding: 1.75rem;
    background: rgba(255, 255, 255, 0.6);
  }
  .day-label {
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    color: var(--verde-hoja);
    font-weight: 600;
  }
  .program-day h3 {
    margin: 0.5rem 0 0.5rem;
    font-size: 1.15rem;
  }
  .program-day p {
    font-size: 0.95rem;
    color: var(--color-text-secondary);
    margin: 0;
    line-height: 1.6;
  }
  .program-link {
    margin: 0;
  }
  .program-link a {
    font-weight: 500;
    font-size: 1rem;
    text-decoration: none;
  }
  .program-link a:hover {
    text-decoration: underline;
  }

  /* ===== Stats ===== */
  .stats-row {
    display: flex;
    justify-content: center;
    gap: 4rem;
    max-width: 800px;
    margin: 0 auto;
    text-align: center;
  }
  .stat {
    flex: 1;
  }
  .stat-num {
    display: block;
    font-family: var(--font-heading);
    font-size: clamp(2.5rem, 5vw, 3.5rem);
    font-weight: 800;
    color: var(--verde-bosque);
    letter-spacing: -0.05em;
    line-height: 1.1;
    margin-bottom: 0.5rem;
  }
  .stat-label {
    display: block;
    font-size: 0.9rem;
    color: var(--color-text-secondary);
    line-height: 1.5;
  }

  /* ===== For Whom Preview ===== */
  .discover-link {
    margin-top: 0.5rem;
  }
  .discover-link a {
    font-weight: 500;
    font-size: 1.05rem;
    text-decoration: none;
  }
  .discover-link a:hover {
    text-decoration: underline;
  }

  /* ===== CTA Final ===== */
  .cta-final {
    padding: 6rem 1.5rem;
    text-align: center;
  }
  .cta-final-inner {
    max-width: 600px;
    margin: 0 auto;
  }
  .cta-final h2 {
    margin: 0 0 1rem;
    font-size: clamp(2rem, 4vw, 2.75rem);
  }
  .cta-final p {
    margin: 0 0 2rem;
    font-size: 1.1rem;
  }

  /* ===== Responsive ===== */
  @media (max-width: 768px) {
    .hero {
      padding: 3.5rem 1.5rem 4rem;
    }
    .hero-sub {
      font-size: 1.05rem;
    }
    .program-grid {
      grid-template-columns: 1fr;
    }
    .stats-row {
      flex-direction: column;
      gap: 2.5rem;
      padding: 0 1.5rem;
    }
    .cta-final {
      padding: 4rem 1.5rem;
    }
  }

  @media (max-width: 480px) {
    .hero-badge {
      font-size: 0.7rem;
    }
  }
</style>
